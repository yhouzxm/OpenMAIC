import{describe,expect,it}from'vitest';import{riskGovernanceMigration}from'@/lib/zhiban/db/migrations/024-risk-governance';describe('stage 11 risk governance',()=>{it('adds mode controls, notifications, learner requests and intervention outcomes',()=>{const sql=riskGovernanceMigration.up.join('\n');for(const value of ['risk_course_controls',"'shadow'",'emergency_stop','risk_notifications','risk_learner_requests','intervention_brief_id','outcome_score','ENABLE ROW LEVEL SECURITY'])expect(sql).toContain(value);});});
