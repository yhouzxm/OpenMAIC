import{describe,expect,it}from'vitest';import{riskInterventionMigration}from'@/lib/zhiban/db/migrations/023-risk-intervention';describe('stage 11 risk schema',()=>{it('stores explainable snapshots, SLA cases, transitions and learner preferences',()=>{const sql=riskInterventionMigration.up.join('\n');for(const value of ['risk_snapshots','confidence','evidence','sources','risk_cases','sla_due_at','risk_case_transitions','risk_support_preferences','risk_evaluate','ENABLE ROW LEVEL SECURITY'])expect(sql).toContain(value);});});
