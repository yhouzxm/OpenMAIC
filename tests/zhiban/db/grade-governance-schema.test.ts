import{describe,expect,it}from'vitest';import{gradeGovernanceMigration}from'@/lib/zhiban/db/migrations/022-grade-governance';describe('stage 10 grade governance',()=>{it('supports special states, publication batches and review requests under RLS',()=>{const sql=gradeGovernanceMigration.up.join('\n');for(const value of ["'absent'","'deferred'","'makeup'",'grade_publication_batches','grade_review_requests','ENABLE ROW LEVEL SECURITY'])expect(sql).toContain(value);});});
