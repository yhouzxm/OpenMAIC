import {describe,expect,it} from 'vitest';import {calculateFinalGrade} from '@/lib/zhiban/grades';
describe('stage 10 weighted final grade contract',()=>{it('uses configured three-category weights and assigns a letter grade',()=>{expect(calculateFinalGrade({formative:80,project:90,final:70},{formativeWeight:40,projectWeight:30,finalWeight:30})).toMatchObject({total:80,letterGrade:'B'});});it('treats a missing category as zero without publishing an invented score',()=>{expect(calculateFinalGrade({formative:100},{formativeWeight:40,projectWeight:30,finalWeight:30}).total).toBe(40);});});
